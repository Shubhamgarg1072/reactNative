// Public API barrel for the home feature.
// Only HomeScreen is exported — all components and hooks are "internal" to this feature.
// This is the index.ts equivalent of Android's internal / implementation visibility.
export { HomeScreen } from './HomeScreen';
